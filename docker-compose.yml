services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./vendor.db
    volumes:
      - ./vendor.db:/app/vendor.db
  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - api
