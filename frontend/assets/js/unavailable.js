(() => { "use strict"; const api = window.VendorIQApi, auth = window.VendorIQAuth; async function init() { if (!api || !auth || !auth.requireAuthentication()) return; try { const user = auth.setUser(await api.me(auth.getAccessToken())); document.getElementById("sessionUser").textContent = `${user.first_name} ${user.last_name}`; } catch (error) { document.getElementById("unavailableMessage").textContent = error.message || "Unable to validate your session."; } document.getElementById("logoutButton").addEventListener("click", () => { auth.clearSession(); location.replace("login.html"); }); } document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", init, { once: true }) : init(); })();
